2026-05-01: Added a focused in-memory `SessionBindingStore` contract under `src/codebase/session_binding.rs` with `tokio::sync::RwLock<HashMap<...>>` ownership and `project_id`-only binding state. RED tests now cover bind/rebind, unbind, and bounded prune; both focused cargo tests fail at the expected `unimplemented!` contract gap, with evidence captured under `.sisyphus/evidence/task-1-*.txt`.
2026-05-01: rmcp `StreamableHttpService` injects `http::request::Parts` into request/notification extensions before dispatch; `MemoryMcpServer::call_tool` can read `RequestContext<RoleServer>.extensions` before `ToolCallContext::new`. Added handler-layer `extract_session_id` logic that reads `mcp-session-id` from those extensions only, returns `None` for empty/headerless contexts, and keeps HTTP transport lifecycle semantics unchanged.
2026-05-01: Added RED `ProjectInfoParams` coverage for additive session-binding actions `bind`, `unbind`, and `binding_status` in `src/server/params.rs` plus a handler-level no-session contract test in `src/server/handler.rs`. The schema deserializers pass, while the tool-surface test stays red because `project_info` still rejects those actions as invalid; evidence captured under `.sisyphus/evidence/task-3-*.txt`.
2026-05-01: Added focused RED contract tests in `src/server/logic/code/mod.rs` named `code_project_resolution_sources` and `code_project_resolution_stale_binding`. They lock response-shape expectations for top-level `project_resolution` on `search_code`, `recall_code`, and `search_symbols`, with source taxonomy `explicit | session_binding | cross_project`, and stale session-binding diagnostics (`reason_code="stale"`, additive `binding_state="stale_binding"`) with no silent broad fallback. Focused failures were captured to `.sisyphus/evidence/task-4-resolution-sources.txt` and `.sisyphus/evidence/task-4-stale-binding.txt`.
2026-05-01: Corrected task-4 RED tests to avoid contradictory setup by introducing a test-only `PlannedProjectResolutionContext` seam in `src/server/logic/code/mod.rs`. Session-binding and stale-binding branches now explicitly model "no explicit project_id + session-bound project" semantics, while explicit branch remains explicit-only. This preserves the planned precedence order (explicit > session binding > cross-project) and prevents stale-binding assertions from incorrectly depending on `project_id: Some(stale_project)`.
2026-05-02: Implemented `SessionBindingStore` as an in-memory `RwLock<HashMap<session_id, SessionBinding>>` with last-write-wins bind/rebind, unbind, status lookup, monotonic millisecond timestamps, and bounded oldest-entry trimming. `AppState` now explicitly owns `session_bindings` in main/test constructors without adding any global active project or persistence. Focused T5 evidence was captured under `.sisyphus/evidence/task-5-*.txt`.
2026-05-01: Task 6 threads handler session identity through a compact `CodeToolContext` instead of passing raw `RequestContext<RoleServer>` below the handler boundary. `call_tool` intercepts `project_info`, `recall_code`, and `search_symbols` to provide the context internally; direct logic entrypoints still default to `None`. Focused handler tests prove session-bound project lookup reaches the code resolver seam and no-session calls keep `project_id=null`; evidence captured in `.sisyphus/evidence/task-6-session-threading.txt` and `.sisyphus/evidence/task-6-no-session.txt`.
2026-05-01: Task 7 implemented additive `project_info` actions `bind`, `unbind`, and `binding_status` inside `project_info_with_session` using `CodeToolContext` + `AppState.session_bindings` only. No-session flows now return success JSON with `reason_code="unsupported"` and `binding: null`; unknown project bind returns machine-readable `reason_code="missing"` via existing missing-project diagnostic helper without writing bindings; binding an indexing project is allowed and returns lifecycle partial diagnostics; `index_project` has no session auto-bind side effects. Evidence: `.sisyphus/evidence/task-7-bind-roundtrip.txt`, `.sisyphus/evidence/task-7-no-session-bind.txt`, `.sisyphus/evidence/task-7-unknown-project-bind.txt`, `.sisyphus/evidence/task-7-indexing-project-bind.txt`, `.sisyphus/evidence/task-7-no-auto-bind-side-effect.txt`.
2026-05-02: Task 8 centralized code-tool project resolution in `src/server/logic/code/mod.rs` so `search_code`, `recall_code`, and `search_symbols` now share precedence `explicit project_id > session binding > cross_project`. Each surface adds top-level `project_resolution`; stale session bindings return success JSON with empty results, `reason_code="stale"`, `binding_state="stale_binding"`, and `summary.partial.reason_code="stale"` without widening to cross-project search. Evidence: `.sisyphus/evidence/task-8-task-4-resolution-sources.txt`, `.sisyphus/evidence/task-8-task-4-stale-binding.txt`, `.sisyphus/evidence/task-8-explicit-overrides.txt`, `.sisyphus/evidence/task-8-session-scopes-search.txt`.
2026-05-02: Task 9 added HTTP `/mcp` QA in `src/transport/http_sse.rs` using in-process axum `oneshot` requests with real MCP JSON-RPC handshake (`initialize` + `notifications/initialized`) and issued `mcp-session-id` capture/reuse. Coverage now proves two issued session IDs are distinct and reused for isolated `project_info(bind)` + `recall_code` scoping, and proves rapid bind/unbind/rebind is last-write-wins before stale-binding checks. Minimal bug fix uncovered by QA: `delete_project` now also removes the project from `ProjectRegistry`, so bind -> delete_project -> search in the same session correctly returns stale diagnostics (`reason_code="stale"`, `binding_state="stale_binding"`) with no cross-project widening. Evidence: `.sisyphus/evidence/task-9-http-two-sessions.txt`, `.sisyphus/evidence/task-9-stale-binding.txt`, `.sisyphus/evidence/task-9-http-focused-tests.txt`.
2026-05-01: Task 10 added regression coverage for code-intelligence backward compatibility: `no_binding_preserves_project_id_none_breadth` confirms explicit `project_id` still wins while no-session/no-binding code retrieval stays `cross_project` with `project_id=null` and includes markers from both indexed projects; `stdio_no_session_binding_unsupported` confirms no-session `project_info(action="bind")` returns `reason_code="unsupported"` with `binding:null`, leaves `AppState.session_bindings` empty, and later no-session recall still stays cross-project. Evidence: `.sisyphus/evidence/task-10-cross-project-compat.txt`, `.sisyphus/evidence/task-10-stdio-no-session.txt`.
12: 2026-05-02: Documented session-scoped project binding in README.md. Established strict resolution precedence (explicit > session > cross-project) and documented the in-memory, session-keyed lifecycle. Verified that stdio mode, auto-bind behavior, and stale binding diagnostics are accurately reflected in operator-facing docs.

### Task 11: Documentation and Audit
- Explicitly documented the project resolution order (Explicit > Session Binding > Breadth Fallback) and session binding lifetime (in-memory, keyed by ).
- Added "Important Security & Scope Notes" to the HTTP configuration section to clarify that remote uploads and header-based binding are not supported, preserving the server-visible path model.
- Documented  diagnostic behavior to ensure operators understand why a bound session might return empty results after a project is deleted.

### Task 11: Documentation and Audit
- Explicitly documented the project resolution order (Explicit > Session Binding > Breadth Fallback) and session binding lifetime (in-memory, keyed by `mcp-session-id`).
- Added "Important Security & Scope Notes" to the HTTP configuration section to clarify that remote uploads and header-based binding are not supported, preserving the server-visible path model.
- Documented `stale_binding` diagnostic behavior to ensure operators understand why a bound session might return empty results after a project is deleted.

### Task 11: Documentation and Audit (Consolidated)
2026-05-02: Documented session-scoped project binding and resolution order in README.md. Established strict resolution precedence: 1. Explicit project_id, 2. Session Binding (keyed by `mcp-session-id`), 3. Breadth Fallback. Documented in-memory lifetime and stdio limitations (`reason_code="unsupported"`). Verified that stale bindings return `reason_code="stale"` and `binding_state="stale_binding"` without silent cross-project fallback. Added security notes confirming only server-visible local paths are supported (no remote uploads, no header/query binding). Audit evidence captured in `.sisyphus/evidence/task-11-docs-resolution-order.txt` and `.sisyphus/evidence/task-11-docs-scope-audit.txt`.
